import { SubmitConfirmationDirective } from './submit-confirmation.directive';

describe('SubmitConfirmationDirective', () => {
  it('should create an instance', () => {
    const directive = new SubmitConfirmationDirective();
    expect(directive).toBeTruthy();
  });
});
